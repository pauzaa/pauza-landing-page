import type { ReactNode } from "react";

export function PhoneMockup({ children }: { children?: ReactNode }) {
  return (
    <div>
      {/* TODO: Phone frame wrapping screenshot */}
      {children}
    </div>
  );
}
